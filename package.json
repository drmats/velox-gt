{
    "dependencies": {
        "gatsby": "4.1.2",
        "react": "17.0.2",
        "react-dom": "17.0.2"
    },
    "devDependencies": {
        "rimraf": "3.0.2"
    },
    "private": true,
    "scripts": {
        "clean:modules": "rimraf gatsby-theme-velox/node_modules && rimraf testbed/node_modules && rimraf yarn.lock && rimraf node_modules"
    },
    "workspaces": [
        "gatsby-theme-velox",
        "testbed"
    ]
}
